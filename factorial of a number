#include<stdio.h>
int main()
{
int n,i,f=1;
printf("enter the num");
scanf("%d",&n);
if(n<0)
{
printf("factorial of negative numbers are not there");
}
else
{
for(i=1;i<=n;i++)
{
f=f*i;
}
printf("factorial of %d is %d",n,f);
}
}
