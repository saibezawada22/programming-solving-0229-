#include<stdio.h>
int main()
{
float side,surface area,volume;
printf("enter length of side");
scanf("%f,&side);
surface area=6*side*side;
volume=pow(side,3);
printf("surface area=%.2f and volume %.2f",surface area,volume);
}
