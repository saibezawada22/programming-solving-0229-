#include<stdio.h>
#include<math.h>
int main()
{
int num,a,r,sum;
sum=0;
printf("enter num");
scanf("%d",&num);
a=num;
while(a>0)
{
r=a%10;
sum=(sum*10)+r;
a=a/10;
}
if(sum==num)
{
printf("num is palindrome");
}
else
{
printf("num is not palindrome");
}
}

